{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "bedrock_schemas.behavior.blocks.minecraft:breathability",
    "description": "Specifies whether the block is breathable by defining if the block is treated as a solid or as air. The default is solid if this component is omitted.",
    "type": "string",
    "default": "solid",
    "enum": ["solid", "air"],
    "required": [
    ],
    "$comment": "version:(1.16.100,*)"
}